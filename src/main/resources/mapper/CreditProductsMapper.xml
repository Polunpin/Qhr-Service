<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.dao.CreditProductsMapper">

    <resultMap id="CreditProductsMap" type="com.tencent.model.CreditProduct">
        <id property="id" column="id"/>
        <result property="bankName" column="bank_name"/>
        <result property="productName" column="product_name"/>
        <result property="productType" column="product_type"/>
        <result property="minAmount" column="min_amount"/>
        <result property="maxAmount" column="max_amount"/>
        <result property="interestRateRange" column="interest_rate_range"/>
        <result property="loanTerm" column="loan_term"/>
        <result property="repaymentMethod" column="repayment_method"/>
        <result property="serviceArea" column="service_area"/>
        <result property="criteriaJson" column="criteria_json"/>
        <result property="status" column="status"/>
        <result property="successRate" column="success_rate"/>
        <result property="createdAt" column="created_at" javaType="java.time.LocalDateTime" />
        <result property="updatedAt" column="updated_at" javaType="java.time.LocalDateTime" />
    </resultMap>

    <sql id="CreditProduct_Columns">
        id, bank_name, product_name, product_type, min_amount, max_amount, interest_rate_range,
        loan_term, repayment_method, service_area, criteria_json, status, success_rate,
        created_at, updated_at
    </sql>

    <select id="getById" resultMap="CreditProductsMap" parameterType="java.lang.Long">
        SELECT <include refid="CreditProduct_Columns"/>
        FROM credit_products
        WHERE id = #{id}
    </select>

    <select id="list" resultMap="CreditProductsMap">
        SELECT <include refid="CreditProduct_Columns"/>
        FROM credit_products
        <where>
            <if test="status != null"> AND status = #{status}</if>
            <if test="productType != null and productType != ''"> AND product_type = #{productType}</if>
            <if test="bankName != null and bankName != ''"> AND bank_name LIKE CONCAT('%', #{bankName}, '%')</if>
        </where>
        ORDER BY id DESC
        <if test="offset != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>

    <select id="count">
        SELECT COUNT(1)
        FROM credit_products
        <where>
            <if test="status != null"> AND status = #{status}</if>
            <if test="productType != null and productType != ''"> AND product_type = #{productType}</if>
            <if test="bankName != null and bankName != ''"> AND bank_name LIKE CONCAT('%', #{bankName}, '%')</if>
        </where>
    </select>

    <select id="findEligibleProducts" resultMap="CreditProductsMap">
        SELECT <include refid="CreditProduct_Columns"/>
        FROM credit_products
        <where>
            AND status = 1
            <if test="expectedAmount != null"> AND min_amount &lt;= #{expectedAmount} AND max_amount &gt;= #{expectedAmount}</if>
            <if test="expectedTerm != null"> AND loan_term &gt;= #{expectedTerm}</if>
            <if test="regionCode != null and regionCode != ''"> AND service_area LIKE CONCAT('%', #{regionCode}, '%')</if>
            <if test="productType != null and productType != ''"> AND product_type = #{productType}</if>
        </where>
        ORDER BY success_rate DESC, id DESC
        <if test="offset != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>

    <select id="countEligibleProducts">
        SELECT COUNT(1)
        FROM credit_products
        <where>
            AND status = 1
            <if test="expectedAmount != null"> AND min_amount &lt;= #{expectedAmount} AND max_amount &gt;= #{expectedAmount}</if>
            <if test="expectedTerm != null"> AND loan_term &gt;= #{expectedTerm}</if>
            <if test="regionCode != null and regionCode != ''"> AND service_area LIKE CONCAT('%', #{regionCode}, '%')</if>
            <if test="productType != null and productType != ''"> AND product_type = #{productType}</if>
        </where>
    </select>

    <insert id="insert" parameterType="com.tencent.model.CreditProduct" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO credit_products (bank_name, product_name, product_type, min_amount, max_amount,
                                     interest_rate_range, loan_term, repayment_method, service_area,
                                     criteria_json, status, success_rate)
        VALUES (#{bankName}, #{productName}, #{productType}, #{minAmount}, #{maxAmount},
                #{interestRateRange}, #{loanTerm}, #{repaymentMethod}, #{serviceArea},
                #{criteriaJson}, #{status}, #{successRate})
    </insert>

    <update id="update" parameterType="com.tencent.model.CreditProduct">
        UPDATE credit_products
        <set>
            <if test="bankName != null">bank_name = #{bankName},</if>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="productType != null">product_type = #{productType},</if>
            <if test="minAmount != null">min_amount = #{minAmount},</if>
            <if test="maxAmount != null">max_amount = #{maxAmount},</if>
            <if test="interestRateRange != null">interest_rate_range = #{interestRateRange},</if>
            <if test="loanTerm != null">loan_term = #{loanTerm},</if>
            <if test="repaymentMethod != null">repayment_method = #{repaymentMethod},</if>
            <if test="serviceArea != null">service_area = #{serviceArea},</if>
            <if test="criteriaJson != null">criteria_json = #{criteriaJson},</if>
            <if test="status != null">status = #{status},</if>
            <if test="successRate != null">success_rate = #{successRate},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE credit_products SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM credit_products WHERE id = #{id}
    </delete>

</mapper>
