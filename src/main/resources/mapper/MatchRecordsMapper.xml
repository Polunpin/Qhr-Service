<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.dao.MatchRecordsMapper">

    <resultMap id="MatchRecordsMap" type="com.tencent.model.MatchRecord">
        <id property="id" column="id"/>
        <result property="matchNo" column="match_no"/>
        <result property="enterpriseId" column="enterprise_id"/>
        <result property="intentionId" column="intention_id"/>
        <result property="productIds" column="product_ids"/>
        <result property="matchScore" column="match_score"/>
        <result property="riskType" column="risk_type"/>
        <result property="riskLevel" column="risk_level"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at" javaType="java.time.LocalDateTime" />
    </resultMap>

    <sql id="MatchRecord_Columns">
        id, match_no, enterprise_id, intention_id, product_ids, match_score,
        risk_type, risk_level, status, created_at
    </sql>

    <select id="getById" resultMap="MatchRecordsMap" parameterType="java.lang.Long">
        SELECT <include refid="MatchRecord_Columns"/>
        FROM match_records
        WHERE id = #{id}
    </select>

    <select id="getByMatchNo" resultMap="MatchRecordsMap" parameterType="java.lang.String">
        SELECT <include refid="MatchRecord_Columns"/>
        FROM match_records
        WHERE match_no = #{matchNo}
    </select>

    <select id="list" resultMap="MatchRecordsMap">
        SELECT <include refid="MatchRecord_Columns"/>
        FROM match_records
        <where>
            <if test="enterpriseId != null"> AND enterprise_id = #{enterpriseId}</if>
            <if test="intentionId != null"> AND intention_id = #{intentionId}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY id DESC
        <if test="offset != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>

    <select id="count">
        SELECT COUNT(1)
        FROM match_records
        <where>
            <if test="enterpriseId != null"> AND enterprise_id = #{enterpriseId}</if>
            <if test="intentionId != null"> AND intention_id = #{intentionId}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
    </select>

    <insert id="insert" parameterType="com.tencent.model.MatchRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO match_records (match_no, enterprise_id, intention_id, product_ids,
                                   match_score, risk_type, risk_level, status)
        VALUES (#{matchNo}, #{enterpriseId}, #{intentionId}, #{productIds},
                #{matchScore}, #{riskType}, #{riskLevel}, #{status})
    </insert>

    <update id="update" parameterType="com.tencent.model.MatchRecord">
        UPDATE match_records
        <set>
            <if test="enterpriseId != null">enterprise_id = #{enterpriseId},</if>
            <if test="intentionId != null">intention_id = #{intentionId},</if>
            <if test="productIds != null">product_ids = #{productIds},</if>
            <if test="matchScore != null">match_score = #{matchScore},</if>
            <if test="riskType != null">risk_type = #{riskType},</if>
            <if test="riskLevel != null">risk_level = #{riskLevel},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE match_records SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM match_records WHERE id = #{id}
    </delete>

</mapper>
